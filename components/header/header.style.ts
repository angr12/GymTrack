import { StyleSheet } from "react-native";
  

const styles = StyleSheet.create({
    btnDim: (dimension: number)=>({
        width: dimension,
        height: dimension,
    }),
    
});

export default styles;